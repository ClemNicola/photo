---
import { Image } from 'astro:assets'
import { getCollection, type CollectionEntry } from 'astro:content'

  export interface Props {
    category: string
  }
  const { category } = Astro.props

  const photos: CollectionEntry<'photos'>[] = await getCollection('photos', (photo: CollectionEntry<'photos'>) =>
    photo.data.category === category
  )
---

<div>
  {photos.map((photo) => (
    <div class="flex justify-center">
      <div class="flex flex-col py-4">
        <h2 class="font-custom font-semi-bold py-4 text-4xl text-custom-black">
          {photo.data.category}
        </h2>
        <div class="flex gap-8 items-end">
          <Image src={photo.data.image} alt={photo.data.title} width='900' height='1250' class='object-cover'/>
          <div class="border-t-2 ">
            <h3 class="font-custom text-xl">
              See more
            </h3>
          </div>
        </div>
      </div>
    </div>
  ))}
</div>
